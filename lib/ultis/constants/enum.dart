enum TextSizes { small, medium, large }

enum OrderState { processing, shipped, deliveded, pending }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  zaloPay,
  momoPay
}

enum NotificationType { order, product, promotion, system }

enum ProductType { single, variable }
